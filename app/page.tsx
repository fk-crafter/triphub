import { Hero, Destinations, Navbar, LogosCarousel } from "@/components";

const Home = () => {
  return (
    <div>
      <Navbar />
      <Hero />
      <LogosCarousel />
      <Destinations />
    </div>
  );
};

export default Home;
